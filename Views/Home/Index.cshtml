@{
    // Keep an index landing page or redirect to login/register as needed
    ViewData["Title"] = "Home";
    Layout = "~/Views/Shared/_Layout_Auth.cshtml";
}

<div class="text-center mt-5">
    <h1>Chào mừng đến với WA ProjectManagement</h1>
    <p>Vui lòng <a asp-controller="Auth" asp-action="Login">Đăng nhập</a> hoặc <a asp-controller="Auth" asp-action="Register">Đăng ký</a> để tiếp tục.</p>
</div>
